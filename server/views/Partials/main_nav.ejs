  <!--Navbar-->
  <div class="cover-container d-flex h-100 p-3 mx-auto flex-column">
    <header class="masthead mb-auto">
      <div class="inner">
        <h3 class="masthead-brand">Workout Tracker</h3>
        <nav class="nav nav-masthead justify-content-center">
          <div class="container-fluid">
            <div id="navbarSupportedContent">
              <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                  <% if(title=="Home") { %>
                      <a class="nav-link active" href="/home">
                   <% } else { %>     
                    <a class="nav-link" href="/home">
                  <% } %>
                    <i class="fa-solid fa-house-chimney"></i> Home</a>
                <li class="nav-item">
                  <% if(title=="Workouts") { %>
                      <a class="nav-link active" href="/workoutlist">
                   <% } else { %>     
                    <a class="nav-link" href="/workoutlist">
                  <% } %>
                    <i class="fa-solid fa-list-check"></i> My Workouts</a>
              </ul>
            </div>
          </div>
        </nav>
      </div>
    </header>